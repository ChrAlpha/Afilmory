// The PhotoViewer is now rendered globally in layout.sync.tsx to ensure immediate opening performance.
// This route component is kept to satisfy the router for /photos/:photoId URLs.
export const Component = () => {
  return null
}
